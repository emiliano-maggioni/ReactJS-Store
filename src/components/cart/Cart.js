import React  from 'react'
import classes from "./Cart.module.scss";

const Cart = (props) => {
  
  return (
    <div className={classes.container} >  
      CART

    </div>
  );
}

export default Cart;
